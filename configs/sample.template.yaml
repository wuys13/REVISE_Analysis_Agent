# Copy beside the two objects. Declare only verified capabilities.
schema_version: 1
sample_id: your_sample
files:
  raw: raw.h5ad
  svc: SVC.h5ad
expression:
  raw: {matrix: X, identity: unknown}
  svc: {matrix: X, identity: unknown}
# X is always finite, nonnegative, unlogged linear expression. Declare only an
# identity supported by provenance; unknown keeps expression analyses unavailable.
columns:
  broad: Level1
  subtype: Level2
  reconstruction: SVC_cluster
label_aliases:
  Mono_Macro: Mono/Macro
spatial:
  key: spatial
  unit: micron
  microns_per_coordinate: 1.0
